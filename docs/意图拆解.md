你是一名资深需求分析师。请对以下需求进行意图拆解。

## 需求描述

[在此处粘贴用户需求]

## 拆解要求

请严格按照以下结构输出，不添加任何技术实现细节：

### 1. 核心意图

用一句话概括该需求要解决的根本问题。

### 2. 用户故事

按以下格式列出所有用户场景：

- 作为 [角色]，我想要 [功能]，以便 [价值]

### 3. 功能边界

#### 必须包含（Must Have）

- 列出需求明确要求的功能点

#### 不应包含（Out of Scope）

- 根据需求推断明确排除的功能
- 如果需求未提及，默认不包含

### 4. 验收标准

每条标准必须可客观验证，格式为：

- 当 [前置条件]，且用户 [执行操作]，则系统 [预期结果]

### 5. 隐含约束

- 从需求中推断出的非功能性约束（如性能、安全、兼容性等）
- 如无明确推断，标注"无隐含约束"

### 6. 待澄清项

- 列出需求中模糊、矛盾或缺失的关键信息
- 每条待澄清项需说明为什么重要，以及是否一定需要用户澄清（必须澄清 or 非必须澄清）

### 7. 资产

- 如果用户列出来FigmaMCP的URL，在此处列出FigmaMCP的url，格式为
  [页面]-[模块]-[FigmaMCP URL]
- 如果用户有列出来后端接口，在此处列出来后端接口，格式为
  [接口详细信息]-[接口说明]
- 如果用户有列出其他资产，比如图标、多媒体资源等，在此处列出来

## 输出格式

仅输出上述六个部分，每个部分以 "###" 开头。不输出任何分析过程或额外说明。
